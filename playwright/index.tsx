import { beforeMount } from '@playwright/experimental-ct-react/hooks';

// Import CSS files needed for the components
import '../src/ui/stateDisplay.css';
import '../src/ui/stateEditor.css';

beforeMount(async () => {
	// Any setup before mounting components
});
